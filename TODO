set time to current time, not last used time
set groups for whole reciept
sortable columns

shop autocomplete
enter in amount adds extra cost row
mulitplier
regular db backups
csv export
regular csv export

configurable header - expand the compilation class' expression field
